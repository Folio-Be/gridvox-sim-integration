export const EditorScreen = () => {
    return (
        <section>
            <h2>2D UV Editor</h2>
            <p>This view will host the canvas-based painting tools and live preview hook.</p>
        </section>
    );
};
